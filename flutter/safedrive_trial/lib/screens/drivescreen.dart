import 'package:flutter/material.dart';

class DriveScreen extends StatelessWidget {
  const DriveScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Current Driving Status'),
    );
  }
}